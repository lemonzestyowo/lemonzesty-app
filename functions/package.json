{
  "main": "lib/index.js",
  "scripts": { // Start of scripts object
    "genkit:start": "genkit start -- tsx --watch src/genkit-sample.ts", // Comma after each line except the last
    "lint": "eslint --ext .js,.ts .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log" // No comma here, as it's the last property in "scripts"
  }, // End of scripts object
  "name": "functions", // Comma required here
  "engines": { // Start of engines object
    "node": "18"  // No comma here, as it's the last property in "engines"
  }, // End of engines object
  "dependencies": { // Start of dependencies object
    "express": "^5.1.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "@genkit-ai/core": "^0.4.0",
    "@genkit-ai/firebase": "^0.4.0",
    "@genkit-ai/vertexai": "^0.4.0",
    "zod": "^3.22.4" // No comma here, as it's the last property in "dependencies"
  }, // End of dependencies object
  "devDependencies": { // Start of devDependencies object
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.29.1",
    "firebase-functions-test": "^3.1.0",
    "tsx": "^4.19.4",
    "typescript": "~5.1.0" // No comma here, as it's the last property in "devDependencies"
  }, // End of devDependencies object
  "private": true // No comma here, as it's the last property in the main object
}
